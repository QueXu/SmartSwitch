<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SmartSwitch</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #e0f7fa;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .device-container {
      display: flex;
      justify-content: center;
      align-items: center;
      flex-wrap: wrap;
      gap: 20px;
      margin-top: 20px;
    }
    .device-card {
      background-color: white;
      border: 1px solid #ddd;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      padding: 20px;
      width: 200px;
      text-align: center;
      transition: transform 0.2s;
    }
    .device-card:hover {
      transform: scale(1.05);
    }
    .device-image {
      width: 100px;
      height: 100px;
      cursor: pointer;
    }
    .device-name {
      font-size: 1.2em;
      margin: 10px 0;
    }
    .add-device {
      background-color: #8bc34a;
      border: none;
      border-radius: 50%;
      width: 50px;
      height: 50px;
      font-size: 2em;
      color: white;
      cursor: pointer;
      transition: background-color 0.3s;
    }
    .add-device:hover {
      background-color: #7cb342;
    }
  </style>
</head>
<body>
  <div class="device-container">
    <% @devices.each do |device| %>
      <div class="device-card">
        <%= link_to image_tag('device_image.png', class: 'device-image', alt: 'Device Image'), device_path(device) %>
        <div class="device-name"><%= device.name %></div>
      </div>
    <% end %>

    <%= link_to new_device_path do %>
      <button class="add-device">+</button>
    <% end %>
  </div>
</body>
</html>
